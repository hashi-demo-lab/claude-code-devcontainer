{
  "mcpServers": {
    "terraform": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "hashicorp/terraform-mcp-server",
        "-v /var/run/docker.sock:/var/run/docker.sock"
      ]
    }
  }
}