{
  "mcpServers": {
    "terraform": {
      "command": "sudo docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "hashicorp/terraform-mcp-server",
        "-v /var/run/docker.sock:/var/run/docker.sock"
      ]
    }
  }
}